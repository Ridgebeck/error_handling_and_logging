// allows to test logging in non release modes
// typically disabled to not log errors with crashlytics while debugging
const bool testLogging = true;

// booleans to define which crash monitoring services to use
const useCrashlytics = true;
const useSentry = true;
